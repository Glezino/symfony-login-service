{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1 style="background-color:white; text-align: center;">Welcome to the home page!</h1><br>

    <div style="background-color:white; text-align: center;">
        <h4>Actions:</h4>
        <button class="btn btn-primary m-2"> 
            <a style="color: white;" href="{{ path('app_register') }}">register</a>
        </button><br>
        <button class="btn btn-secondary m-2 "> 
            <a style="color: white;" href="{{ path('app_login') }}">login</a>
        </button>
        <button class="btn btn-secondary m-2"> 
            <a style="color: white;" href="{{ path('app_logout') }}">logout</a>
        </button>
        <br><button class="btn btn-warning m-2">
            <a style="color: black;" href="{{ path('app_user_index') }}">users info (ONLY ADMIN)</a>
        </button>
    <div>

</div>
{% endblock %}
